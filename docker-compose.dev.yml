# Local DEV infrastructure for THIS machine.
# Brings up: MySQL (app DB) + Redis + RabbitMQ.
#
# NOTE: The app uses MySQL (Prisma). Redis & RabbitMQ are started per request
# and sit idle/ready for future work — no app code consumes them yet.
#
# Ports are deliberately offset from credit-core (3307/6379/5672) so BOTH projects
# can run on this machine at the same time without clashing.
#
#   App DB:    mysql://root:root@localhost:3308/fininvest
#   Redis:     redis://localhost:6380
#   RabbitMQ:  amqp://guest:guest@localhost:5673   (mgmt UI: http://localhost:15673)
#
# Usage:
#   docker compose -f docker-compose.dev.yml up -d
#   docker compose -f docker-compose.dev.yml down        # stop (keeps data)
#   docker compose -f docker-compose.dev.yml down -v      # stop + wipe volumes
name: fininvest-dev

services:
  mysql:
    image: mysql:8.4
    container_name: fininvest-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: fininvest
    # lower_case_table_names=1 matches Windows/dev MySQL (case-insensitive table names),
    # so the project's migrations (which mix `Document`/`document` casing) replay cleanly.
    command: ['--mysql-native-password=ON', '--lower-case-table-names=1']
    ports:
      - '3308:3306'   # host 3308 -> container 3306 (3307 is credit-core, 3306 is a local MySQL)
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost', '-uroot', '-proot']
      interval: 5s
      timeout: 5s
      retries: 20

  redis:
    image: redis:7-alpine
    container_name: fininvest-redis
    restart: unless-stopped
    ports:
      - '6380:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 12

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: fininvest-rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - '5673:5672'    # AMQP
      - '15673:15672'  # management UI
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ['CMD', 'rabbitmq-diagnostics', '-q', 'ping']
      interval: 10s
      timeout: 5s
      retries: 12

volumes:
  mysql_data:
  redis_data:
  rabbitmq_data:
