# dependencies
node_modules/
.pnp
.pnp.js

# builds
dist/
build/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# TLS certs (issued on the server, never committed)
deploy/nginx/certs/

# uploads / generated files
uploads/
generated/
*.local

# prisma
apps/backend/prisma/*.db

# logs
*.log
npm-debug.log*

# editor / os
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# vite
.vite/

# python (skill CLI)
__pycache__/
*.pyc

# tesseract traineddata (bundled into the prod image at build; dev fetches via `npm run setup:ocr`)
*.traineddata
apps/backend/tessdata/

# passport scan fixtures — real documents (PII), kept local only for the OCR integration test
apps/backend/test-fixtures/

# Local output of `npm run render:case` — real client data, never committed.
rendered/
